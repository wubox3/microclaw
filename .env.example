# --- Anthropic (default provider) ---

# Option 1: Anthropic API key (direct)
# ANTHROPIC_API_KEY=your-api-key-here

# Option 2: OAuth token (manual override)
# ANTHROPIC_AUTH_TOKEN=your-oauth-token-here

# Option 3 (default): Claude Code OAuth from macOS Keychain
# No env vars needed - just have Claude Code installed and logged in

# --- OpenRouter (alternative provider) ---
# Set agent.provider: "openrouter" in eclaw.config.yaml to use
# OPENROUTER_API_KEY=sk-or-v1-xxxxx

# --- Telegram ---
# Bot token from @BotFather (e.g. 123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11)
# TELEGRAM_BOT_TOKEN=your-bot-token-here

# --- Slack ---
# Bot User OAuth Token (xoxb-...) from Slack App > OAuth & Permissions
# SLACK_BOT_TOKEN=xoxb-your-bot-token-here
# App-Level Token (xapp-...) from Slack App > Basic Information > App-Level Tokens
# Requires connections:write scope for Socket Mode
# SLACK_APP_TOKEN=xapp-your-app-token-here

# --- WhatsApp ---
# Your WhatsApp phone number in E.164 format (e.g. +14155551234)
# WHATSAPP_PHONE_NUMBER=+1XXXXXXXXXX

# --- Signal ---
# Requires signal-cli running in REST API mode (https://github.com/bbernhard/signal-cli-rest-api)
# Base URL for signal-cli REST API (default: http://localhost:8080)
# SIGNAL_CLI_URL=http://localhost:8080
# Your registered Signal phone number in E.164 format (e.g. +14155551234)
# SIGNAL_PHONE_NUMBER=+1XXXXXXXXXX
